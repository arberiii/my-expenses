'use strict';

describe('myExpenses.expenses module', function() {

  beforeEach(module('myExpenses.expenses'));

  describe('expenses controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ExpensesCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});