'use strict';

describe('myExpenses.payments module', function() {

  beforeEach(module('myExpenses.payments'));

  describe('payments controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('PaymentsCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});